---
title: Introduction
description: Next.js is a React framework for building full-stack web applications.
---

# Welcome to Next.js

Next.js is a React framework that gives you building blocks to create fast, full-stack web applications. It handles the tooling and configuration needed for React and provides structure, features, and optimizations for production.

## Why Next.js?

<Cards>
  <Card title="Server Components" description="Render on the server for faster loads and smaller bundles." href="/documentation/data-fetching/overview" icon="Server" />
  <Card title="File-Based Routing" description="Create routes by adding files to the app/ directory." href="/documentation/routing/pages-and-layouts" icon="FolderTree" />
  <Card title="Built-in Optimizations" description="Automatic image, font, and script optimizations." href="/documentation/quickstart" icon="Zap" />
  <Card title="Full-Stack Ready" description="API Routes, Server Actions, and middleware built in." href="/api-reference/api/route-handlers" icon="Layers" />
</Cards>

## How it works

<Mermaid chart="graph LR;
    A[React Components] --> B[Next.js];
    B --> C{Rendering};
    C -->|Server| D[HTML + streaming];
    C -->|Client| E[Interactive UI];
    D --> F[Fast initial load];
    E --> F;
    F --> G[Production app];" />

## Project structure

A typical Next.js 15 project looks like this:

<Files>
  <Folder name="my-app" defaultOpen>
    <File name="next.config.ts" />
    <File name="package.json" />
    <File name="tsconfig.json" />
    <Folder name="app" defaultOpen>
      <File name="layout.tsx" />
      <File name="page.tsx" />
      <File name="globals.css" />
      <Folder name="dashboard" defaultOpen>
        <File name="page.tsx" />
        <File name="layout.tsx" />
      </Folder>
      <Folder name="api" defaultOpen>
        <Folder name="hello" defaultOpen>
          <File name="route.ts" />
        </Folder>
      </Folder>
    </Folder>
    <Folder name="public" defaultOpen>
      <File name="favicon.ico" />
      <File name="logo.svg" />
    </Folder>
  </Folder>
</Files>

## Quick example

<Tabs items={["Server Component", "Client Component"]}>
  <Tab value="Server Component">
    Server Components render on the server and send HTML to the client — no JavaScript needed on the client side.

    ```tsx title="app/page.tsx"
    export default async function Page() {
      const data = await fetch('https://api.example.com/posts')
      const posts = await data.json()

      return (
        <ul>
          {posts.map((post) => (
            <li key={post.id}>{post.title}</li>
          ))}
        </ul>
      )
    }
    ```
  </Tab>
  <Tab value="Client Component">
    Client Components add interactivity. They render on the client and can use hooks, effects, and browser APIs.

    ```tsx title="app/counter.tsx"
    'use client'

    import { useState } from 'react'

    export default function Counter() {
      const [count, setCount] = useState(0)

      return (
        <button onClick={() => setCount(count + 1)}>
          Count: {count}
        </button>
      )
    }
    ```
  </Tab>
</Tabs>

## Get started

<Cards>
  <Card title="Installation" description="Set up a new Next.js project from scratch." href="/documentation/installation" icon="Download" />
  <Card title="Quickstart" description="Build your first page in under 5 minutes." href="/documentation/quickstart" icon="Rocket" />
  <Card title="Routing" description="Learn how file-based routing works." href="/documentation/routing/pages-and-layouts" icon="Route" />
  <Card title="Deployment" description="Ship to production on any platform." href="/deployment/deployment/overview" icon="Globe" />
</Cards>
